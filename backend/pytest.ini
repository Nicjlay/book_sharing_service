[pytest]
# asyncio_mode = auto: все async-тесты запускаются без @pytest.mark.asyncio
asyncio_mode = auto
# src/ добавляется в PYTHONPATH — пакеты импортируются без src. префикса
pythonpath = src
testpaths = src/tests
# Короткий трейсбек + показывать slow-тесты
addopts = --tb=short -q
